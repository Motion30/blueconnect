import 'package:flutter/material.dart';

class Community extends StatelessWidget {
    static const screenId = 'community_screen';

    @override
    Widget build(BuildContext context) {
        return Container(
            child: Center(
                child: Text("Groups and Community"),
            ),
        );
    }
}
