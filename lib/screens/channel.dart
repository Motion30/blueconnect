import 'package:flutter/material.dart';

class Channel extends StatelessWidget {
    static const screenId = 'channel_screen';

    @override
    Widget build(BuildContext context) {
        return Container();
    }
}
